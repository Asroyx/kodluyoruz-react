const users = [{ name: "Mehnet" }, { name: "Murat" }, { name: "Sevilay" }];

// push
// users.push("Ayşe");
// users.push("Fatma");

// console.log(users);

// map
users.map((item) => {
  console.log(item.name);
});
